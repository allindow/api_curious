
<%= image_tag current_user.profile_picture, {class: "img-circle"} %>
<h1><%= @user.username %>'s feed</h1>
<% @media_feed.each do |media_item| %>
  <img src="<%= media_item.images.thumbnail.url %>">
<% end %>
